var slider = tns({
  container: ".homeBanner__slider",
  items: 1,
  speed: 200,
  slideBy: "page",
  autoplay: true,
  navPosition: "bottom",
  axis: "vertical",
  controls: false,
});

var slider = tns({
  container: ".services__slider",
  items: 3,
  navPosition: "bottom",
  controls: false,
  swipeAngle: false,
  speed: 400,
});
